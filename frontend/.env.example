# Copy this file to .env.local for local development, or set the variable
# in the Vercel dashboard for production.
# The address of the FastAPI backend (no trailing slash).
VITE_API_URL=http://localhost:8000
