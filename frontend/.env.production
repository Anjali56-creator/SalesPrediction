# Used automatically by `vite build` (which is what Vercel runs).
# This is the public address of the deployed FastAPI backend on Render.
# It is not a secret, so it is safe to keep in the repository.
# If VITE_API_URL is also set in the Vercel dashboard, that value wins.
VITE_API_URL=https://sales-prediction-api-l700.onrender.com
